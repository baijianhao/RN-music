const defaultState = {
  source: '',
  poster: '',
  duration: 0,
  playing: false,
  currentTime: 0,
  isSliding: false,
  isEnding: false
}
export default reducer(state = defaultState, action) {
  return state
}